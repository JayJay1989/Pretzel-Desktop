#import "settings-fragment.graphql"

mutation SetSettings($attributes: SettingsAttributes!) {
    updateSettings(attributes: $attributes) {
        user {
            id
            settings {
                ...Settings
            }
        }
        errors
    }
}
