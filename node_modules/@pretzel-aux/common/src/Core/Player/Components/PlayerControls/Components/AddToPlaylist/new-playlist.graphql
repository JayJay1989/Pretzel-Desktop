
mutation PlayerControlsNewPlaylist($attributes: PlaylistAttributes!) {
    createPlaylist(attributes: $attributes) {
        playlist {
            id
            name
            description
            tracks @connection(key: "tracks"){
              edges{
                node{
                  id
                }
              }
            }
        }
        errors
    }
}
