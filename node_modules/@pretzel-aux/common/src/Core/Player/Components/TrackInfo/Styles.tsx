import styled from 'styled-components/macro';

export const Info = styled.p`
  margin-top: 4px;
  margin-bottom: 4px;
  font-size: 14px;
  line-height: 16px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;
