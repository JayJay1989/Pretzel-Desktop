#import "play-token-fragment.graphql"
query CurrentPlayToken {
    currentUser {
        id
        activeSegment {
            __typename
            id
            name
        }
        currentToken {
            ...PlayTokenFragment
        }
    }
}
