#import "play-token-fragment.graphql"
#import "active-segment-fragment.graphql"
query CurrentPlayTokenQuery {
    currentUser {
        id
        activeSegment {
            ...ActiveSegmentFragment
        }
        currentToken {
            ...PlayTokenFragment
        }
    }
}
