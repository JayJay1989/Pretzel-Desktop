#import "active-segment-fragment.graphql"
mutation SetActiveSegment($id: String!) {
    setActiveSegment(id: $id) {
        user {
            id
            activeSegment {
                ...ActiveSegmentFragment
            }
        }
    }
}
