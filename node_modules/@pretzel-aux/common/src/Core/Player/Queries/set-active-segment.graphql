mutation SetActiveSegment($id: String!) {
    setActiveSegment(id: $id) {
        user {
            id
            activeSegment {
                __typename
                id
                name
            }
        }
    }
}
