mutation SetTrackLiked($trackId: ID!, $value: Boolean!) {
    likeTrack(trackId: $trackId, value: $value) {
        track {
            id
            self {
                liked
            }
        }
    }
}
