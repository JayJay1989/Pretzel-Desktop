query UserPlaylistsListQuery($after: String) {
  userPlaylists(after: $after, first: 30) @connection(key: "userPlaylists") {
    edges {
      cursor
      node {
        id
        name
        artworkGuid
        description
        stats {
          totalCount
          duration
          popularity
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
      startCursor
    }
  }
}
