import { BasicTrack } from '../../Core/Player/Queries/schema';

interface TrackEdge {
  node: BasicTrack;
}

export interface SegmentQuery {
  segment: {
    id: string;
    name: string;
    stats: {
      duration: number;
      popularity: number;
      totalCount: number;
    };
    tracks: {
      edges: TrackEdge[];
    };
  };

}
