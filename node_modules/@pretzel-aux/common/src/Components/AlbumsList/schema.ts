import { List } from '../../Core/GraphQL';

export interface Album {
  id: string;
  artworkGuid: string;
  name: string;
  stats: {
    duration: number;
    totalCount: number;
    popularity: number;
  };
}

export interface AlbumsListQuery {
  albums: List<Album>;
}
