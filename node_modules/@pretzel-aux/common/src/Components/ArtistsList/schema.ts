import { List } from '../../Core/GraphQL';

export interface Artist {
  id: string;
  artworkGuid: string;
  name: string;
  stats: {
    duration: number;
    totalCount: number;
    popularity: number;
  };
}

export interface ArtistsListQuery {
  artists: List<Artist>;
}
