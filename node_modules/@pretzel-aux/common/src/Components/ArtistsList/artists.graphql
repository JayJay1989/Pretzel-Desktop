query ArtistsList($after: String) {
    artists(after: $after, first: 30) {
        edges {
            node {
                id
                artworkGuid
                name
                stats {
                    totalCount
                    duration
                    popularity
                }
            }
        }
        pageInfo {
            endCursor
            hasNextPage
            hasPreviousPage
            startCursor
        }
    }
}
