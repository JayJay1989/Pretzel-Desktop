import * as React from 'react';
import { Configure, Index } from 'react-instantsearch-dom';
import { AlbumHit } from './Components/AlbumHit';
import { ArtistHit } from './Components/ArtistHit';
import { SearchMusicList } from './Components/SearchMusicList';
import { SearchList } from './Components/SearchList';

import './style.css';

function SearchImplementation() {
  return (
    <div style={{ overflow: 'auto', padding: '30px' }}>
      <Index indexName="Release_production">
        <Configure hitsPerPage={6} />
        <SearchList title="Albums" hitComponent={AlbumHit} />
      </Index>
      <br/>
      <Index indexName="Artist_production">
        <Configure hitsPerPage={6} />
        <SearchList title="Artists" hitComponent={ArtistHit} />
      </Index>
      {/* <br />
      <Index indexName="Track_production">
        <Configure hitsPerPage={15} />
        <SearchMusicList title="Tracks" />
      </Index> */}
    </div>
  );
}

export const Search = SearchImplementation;
