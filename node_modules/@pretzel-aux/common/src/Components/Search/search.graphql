#import "../../Core/Player/Queries/basic-track-fragment.graphql"
query Search($guids: String!) {
    search(guids: $guids) {
        id
        tracks {
            edges{
                node {
                    ...BasicTrackFragment
                }
            }
        }
    }
}
