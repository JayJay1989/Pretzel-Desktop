#import "../../Core/Player/Queries/basic-track-fragment.graphql"
query SaturateTracksQuery($guids: String!) {
    saturateTracks(guids: $guids) {
        id
        ...BasicTrackFragment
    }
}
