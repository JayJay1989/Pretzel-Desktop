import { List } from '../../Core/GraphQL';

export interface Label {
  id: string;
  artworkGuid: string;
  name: string;
  stats: {
    duration: number;
    totalCount: number;
    popularity: number;
  };
}

export interface LabelsListQuery {
  labels: List<Label>;
}
