import { List } from '../../Core/GraphQL';

export interface Station {
  id: string;
  name: string;
  description: string;
  icon: string;
  color1: string;
  color2: string;
  stats: {
    duration: number;
    totalCount: number;
    popularity: number;
  };
}

export interface StationsListQuery {
  stations: List<Station>;
}
