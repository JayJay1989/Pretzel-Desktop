#import "../../Core/Player/Queries/basic-track-fragment.graphql"
query BlacklistedTracks($after: String!) {
    blacklistedTracks(after: $after, first: 30) {
        edges {
            cursor
            node {
                ...BasicTrackFragment
            }
        }
        pageInfo {
            endCursor
            hasNextPage
            hasPreviousPage
            startCursor
        }
    }
}
