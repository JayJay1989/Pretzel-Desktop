#import "../../Core/Player/Queries/basic-track-fragment.graphql"
query LikedTracks($after: String!) {
    likedTracks(after: $after, first: 30) {
        edges {
            cursor
            node {
                ...BasicTrackFragment
            }
        }
        pageInfo {
            endCursor
            hasNextPage
            hasPreviousPage
            startCursor
        }
    }
}
